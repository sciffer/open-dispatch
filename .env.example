# ===========================================
# SLACK CONFIGURATION (for Slack bots)
# ===========================================
# Required for: src/bot.js, src/opencode-bot.js

# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Signing Secret (from app settings)
SLACK_SIGNING_SECRET=your-signing-secret

# Slack App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# ===========================================
# MICROSOFT TEAMS CONFIGURATION (for Teams bots)
# ===========================================
# Required for: src/teams-bot.js, src/teams-opencode-bot.js
# Get these from Teams Developer Portal (dev.teams.microsoft.com)

# Microsoft App ID (Bot ID from Teams Developer Portal → Tools → Bot management)
MICROSOFT_APP_ID=your-bot-id-here

# Microsoft App Password (client secret from Bot management)
MICROSOFT_APP_PASSWORD=your-client-secret-here

# Bot server port (default: 3978)
PORT=3978

# REQUIRED: Tenant ID for Teams Developer Portal bots (single-tenant by default)
# Find at: Azure Portal → Azure Active Directory → Overview → Tenant ID
MICROSOFT_APP_TENANT_ID=your-tenant-id-here

# ===========================================
# DISCORD CONFIGURATION (for Discord bots)
# ===========================================
# Required for: src/discord-bot.js, src/discord-opencode-bot.js
# See DISCORD_SETUP.md for detailed instructions

# Discord Bot Token (from Discord Developer Portal)
DISCORD_BOT_TOKEN=your-bot-token-here

# Discord Application Client ID (from Discord Developer Portal)
DISCORD_CLIENT_ID=your-client-id-here

# Optional: Guild ID for faster slash command registration during development
# Commands registered to a guild are available instantly
# Global commands can take up to 1 hour to propagate
# DISCORD_GUILD_ID=your-guild-id-here

# ===========================================
# OPENCODE CONFIGURATION (for OpenCode bots)
# ===========================================
# Required for: src/opencode-bot.js, src/teams-opencode-bot.js, src/discord-opencode-bot.js

# Optional: Specify which model to use (e.g., anthropic/claude-sonnet-4-20250514, openai/gpt-4o)
# If not set, OpenCode uses its default model
# OPENCODE_MODEL=anthropic/claude-sonnet-4-20250514

# Optional: Full path to OpenCode binary (if not in PATH)
# OPENCODE_PATH=/home/yourusername/.opencode/bin/opencode

# ===========================================
# SPRITE CONFIGURATION (Cloud VM execution on Fly.io)
# ===========================================
# Required for: src/sprite-bot.js, src/sprite-core.js
# Sprites are ephemeral Fly Machines for running agents in isolated environments
# See SPRITE_SETUP.md for full setup guide

# Chat provider to use: slack, teams, or discord
CHAT_PROVIDER=slack

# Fly.io API token (from: fly tokens create deploy -x 999999h)
FLY_API_TOKEN=your-fly-api-token

# Fly app name for Sprite Machines
FLY_SPRITE_APP=your-fly-app-name

# Default Docker image for Sprites (your agent image with sidecar installed)
SPRITE_IMAGE=registry.fly.io/your-app/agent:latest

# Optional: Webhook callback URL (default: http://open-dispatch.internal:8080)
# Uses Fly.io 6PN private networking by default
# OPEN_DISPATCH_URL=http://open-dispatch.internal:8080

# Optional: Webhook server port (default: 8080)
# WEBHOOK_PORT=8080

# Optional: Preferred Fly.io region (default: iad)
# FLY_REGION=iad

# Optional: Agent type to run in Sprites: 'claude' or 'opencode' (default: claude)
# SPRITE_AGENT_TYPE=claude

# These env vars are passed through to Sprites automatically:
# GH_TOKEN          — GitHub token for private repos and gh CLI (in Sprites)
# ANTHROPIC_API_KEY — For Claude-based agents (in Sprites)
# DATABASE_URL      — If your agents need database access (in Sprites)

# ===========================================
# GITHUB ACTIONS SPRITE CONFIGURATION
# ===========================================
# Alternative sandbox for Sprite mode — runs agents via GitHub Actions workflows
# instead of Fly Machines. Set SPRITE_SANDBOX=github to enable.
# One-shot jobs only (no persistent sessions). Output fetched via GH API.
#
# Required for: src/sprite-bot.js, src/sprite-core.js (when SPRITE_SANDBOX=github)

# Sandbox type: 'fly' (default) or 'github'
# SPRITE_SANDBOX=github

# GitHub personal access token with 'workflow' scope
# GITHUB_SPRITE_TOKEN=ghp_your-token

# GitHub repository to trigger the workflow in
# GITHUB_SPRITE_OWNER=your-org
# GITHUB_SPRITE_REPO=your-repo

# Workflow file path in the repo (default: .github/workflows/open-dispatch-sprite.yml)
# GITHUB_SPRITE_WORKFLOW=.github/workflows/open-dispatch-sprite.yml

# Branch to run on (default: main)
# GITHUB_SPRITE_BRANCH=main

# ===========================================
# WHICH BOT TO RUN?
# ===========================================
#
# LOCAL MODE (agents run on your machine):
# Slack + Claude Code:    npm start                    (src/bot.js)
# Slack + OpenCode:       npm run start:opencode       (src/opencode-bot.js)
# Teams + Claude Code:    npm run start:teams          (src/teams-bot.js)
# Teams + OpenCode:       npm run start:teams:opencode (src/teams-opencode-bot.js)
# Discord + Claude Code:  npm run start:discord        (src/discord-bot.js)
# Discord + OpenCode:     npm run start:discord:opencode (src/discord-opencode-bot.js)
#
# SPRITE MODE (agents run in sandboxed environments):
# Any provider + Sprite:  npm run start:sprite         (src/sprite-bot.js)
#   Set CHAT_PROVIDER=slack|teams|discord to choose provider
#   Set SPRITE_SANDBOX=fly|github to choose sandbox (default: fly)
